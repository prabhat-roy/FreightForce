apiVersion: v1
kind: Service
metadata:
  name: freight-booking
  namespace: freightforce
  labels: { app.kubernetes.io/name: freight-booking, freightforce.io/domain: customs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: freight-booking }
