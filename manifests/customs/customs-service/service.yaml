apiVersion: v1
kind: Service
metadata:
  name: customs-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: customs-service, freightforce.io/domain: customs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: customs-service }
