apiVersion: apps/v1
kind: Deployment
metadata:
  name: customs-service
  namespace: freightforce
  labels:
    app.kubernetes.io/name: customs-service
    app.kubernetes.io/part-of: freightforce
    freightforce.io/domain: customs
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: customs-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: customs-service, freightforce.io/domain: customs }
    spec:
      serviceAccountName: customs-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: customs-service
          image: harbor.freightforce.internal/freightforce/customs-service:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: customs-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
