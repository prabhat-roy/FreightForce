apiVersion: v1
kind: Service
metadata:
  name: compliance-checker
  namespace: freightforce
  labels: { app.kubernetes.io/name: compliance-checker, freightforce.io/domain: customs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50063, targetPort: 50063 }]
  selector: { app.kubernetes.io/name: compliance-checker }
