apiVersion: v1
kind: Service
metadata:
  name: carrier-performance
  namespace: freightforce
  labels: { app.kubernetes.io/name: carrier-performance, freightforce.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: carrier-performance }
