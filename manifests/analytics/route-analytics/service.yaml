apiVersion: v1
kind: Service
metadata:
  name: route-analytics
  namespace: freightforce
  labels: { app.kubernetes.io/name: route-analytics, freightforce.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: route-analytics }
