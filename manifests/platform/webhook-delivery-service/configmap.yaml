apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: webhook-delivery-service, freightforce.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"
