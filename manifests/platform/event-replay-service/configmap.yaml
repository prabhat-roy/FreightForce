apiVersion: v1
kind: ConfigMap
metadata:
  name: event-replay-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: event-replay-service, freightforce.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51009"
