apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: geo-address-service, freightforce.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
