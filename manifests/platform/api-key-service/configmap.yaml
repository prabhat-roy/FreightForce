apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: api-key-service, freightforce.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
