apiVersion: v1
kind: Service
metadata:
  name: rfq-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: rfq-service, freightforce.io/domain: procurement }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50000, targetPort: 50000 }]
  selector: { app.kubernetes.io/name: rfq-service }
