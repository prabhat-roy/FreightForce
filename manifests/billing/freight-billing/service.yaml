apiVersion: v1
kind: Service
metadata:
  name: freight-billing
  namespace: freightforce
  labels: { app.kubernetes.io/name: freight-billing, freightforce.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: freight-billing }
