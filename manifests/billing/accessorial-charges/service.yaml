apiVersion: v1
kind: Service
metadata:
  name: accessorial-charges
  namespace: freightforce
  labels: { app.kubernetes.io/name: accessorial-charges, freightforce.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: accessorial-charges }
