apiVersion: v1
kind: Service
metadata:
  name: scorecard-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: scorecard-service, freightforce.io/domain: supplier-portal }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: scorecard-service }
