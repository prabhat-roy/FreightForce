apiVersion: v1
kind: Service
metadata:
  name: onboarding-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: onboarding-service, freightforce.io/domain: supplier-portal }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: onboarding-service }
