apiVersion: apps/v1
kind: Deployment
metadata:
  name: onboarding-service
  namespace: freightforce
  labels:
    app.kubernetes.io/name: onboarding-service
    app.kubernetes.io/part-of: freightforce
    freightforce.io/domain: supplier-portal
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: onboarding-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: onboarding-service, freightforce.io/domain: supplier-portal }
    spec:
      serviceAccountName: onboarding-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: onboarding-service
          image: harbor.freightforce.internal/freightforce/onboarding-service:latest
          ports: [{ containerPort: 50121, name: http }]
          envFrom: [{ configMapRef: { name: onboarding-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
