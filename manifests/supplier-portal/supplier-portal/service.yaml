apiVersion: v1
kind: Service
metadata:
  name: supplier-portal
  namespace: freightforce
  labels: { app.kubernetes.io/name: supplier-portal, freightforce.io/domain: supplier-portal }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: supplier-portal }
