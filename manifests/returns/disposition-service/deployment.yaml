apiVersion: apps/v1
kind: Deployment
metadata:
  name: disposition-service
  namespace: freightforce
  labels:
    app.kubernetes.io/name: disposition-service
    app.kubernetes.io/part-of: freightforce
    freightforce.io/domain: returns
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: disposition-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: disposition-service, freightforce.io/domain: returns }
    spec:
      serviceAccountName: disposition-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: disposition-service
          image: harbor.freightforce.internal/freightforce/disposition-service:latest
          ports: [{ containerPort: 50081, name: http }]
          envFrom: [{ configMapRef: { name: disposition-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
