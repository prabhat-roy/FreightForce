apiVersion: v1
kind: Service
metadata:
  name: disposition-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: disposition-service, freightforce.io/domain: returns }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: disposition-service }
