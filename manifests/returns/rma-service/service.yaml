apiVersion: v1
kind: Service
metadata:
  name: rma-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: rma-service, freightforce.io/domain: returns }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: rma-service }
