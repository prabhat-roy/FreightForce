apiVersion: v1
kind: Service
metadata:
  name: pick-pack-service
  namespace: freightforce
  labels: { app.kubernetes.io/name: pick-pack-service, freightforce.io/domain: warehouse }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: pick-pack-service }
