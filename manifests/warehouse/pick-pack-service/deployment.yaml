apiVersion: apps/v1
kind: Deployment
metadata:
  name: pick-pack-service
  namespace: freightforce
  labels:
    app.kubernetes.io/name: pick-pack-service
    app.kubernetes.io/part-of: freightforce
    freightforce.io/domain: warehouse
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pick-pack-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pick-pack-service, freightforce.io/domain: warehouse }
    spec:
      serviceAccountName: pick-pack-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pick-pack-service
          image: harbor.freightforce.internal/freightforce/pick-pack-service:latest
          ports: [{ containerPort: 50022, name: http }]
          envFrom: [{ configMapRef: { name: pick-pack-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
