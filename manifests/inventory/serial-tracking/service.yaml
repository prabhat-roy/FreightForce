apiVersion: v1
kind: Service
metadata:
  name: serial-tracking
  namespace: freightforce
  labels: { app.kubernetes.io/name: serial-tracking, freightforce.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: serial-tracking }
