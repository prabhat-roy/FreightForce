apiVersion: v1
kind: Service
metadata:
  name: expiry-management
  namespace: freightforce
  labels: { app.kubernetes.io/name: expiry-management, freightforce.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50042, targetPort: 50042 }]
  selector: { app.kubernetes.io/name: expiry-management }
