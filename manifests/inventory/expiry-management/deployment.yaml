apiVersion: apps/v1
kind: Deployment
metadata:
  name: expiry-management
  namespace: freightforce
  labels:
    app.kubernetes.io/name: expiry-management
    app.kubernetes.io/part-of: freightforce
    freightforce.io/domain: inventory
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: expiry-management } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: expiry-management, freightforce.io/domain: inventory }
    spec:
      serviceAccountName: expiry-management
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: expiry-management
          image: harbor.freightforce.internal/freightforce/expiry-management:latest
          ports: [{ containerPort: 50042, name: http }]
          envFrom: [{ configMapRef: { name: expiry-management } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
