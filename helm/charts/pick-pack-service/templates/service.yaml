apiVersion: v1
kind: Service
metadata:
  name: {{ include "pick-pack-service.fullname" . }}
  labels: {{- include "pick-pack-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pick-pack-service.selectorLabels" . | nindent 4 }}
