apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-bff.fullname" . }}
  labels: {{- include "driver-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "driver-bff.selectorLabels" . | nindent 4 }}
