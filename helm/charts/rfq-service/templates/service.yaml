apiVersion: v1
kind: Service
metadata:
  name: {{ include "rfq-service.fullname" . }}
  labels: {{- include "rfq-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rfq-service.selectorLabels" . | nindent 4 }}
