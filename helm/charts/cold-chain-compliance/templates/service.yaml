apiVersion: v1
kind: Service
metadata:
  name: {{ include "cold-chain-compliance.fullname" . }}
  labels: {{- include "cold-chain-compliance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cold-chain-compliance.selectorLabels" . | nindent 4 }}
