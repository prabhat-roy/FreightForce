apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance-checker.fullname" . }}
  labels: {{- include "compliance-checker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "compliance-checker.selectorLabels" . | nindent 4 }}
