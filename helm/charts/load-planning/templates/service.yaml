apiVersion: v1
kind: Service
metadata:
  name: {{ include "load-planning.fullname" . }}
  labels: {{- include "load-planning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "load-planning.selectorLabels" . | nindent 4 }}
