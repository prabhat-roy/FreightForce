cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: freightforce-load-planning@{{ .Values.gcp.project }}.iam.gserviceaccount.com
