apiVersion: v1
kind: Service
metadata:
  name: {{ include "carrier-selection.fullname" . }}
  labels: {{- include "carrier-selection.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "carrier-selection.selectorLabels" . | nindent 4 }}
