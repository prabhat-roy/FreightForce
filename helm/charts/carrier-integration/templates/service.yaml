apiVersion: v1
kind: Service
metadata:
  name: {{ include "carrier-integration.fullname" . }}
  labels: {{- include "carrier-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "carrier-integration.selectorLabels" . | nindent 4 }}
