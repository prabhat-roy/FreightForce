apiVersion: v1
kind: Service
metadata:
  name: {{ include "cold-chain-service.fullname" . }}
  labels: {{- include "cold-chain-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cold-chain-service.selectorLabels" . | nindent 4 }}
