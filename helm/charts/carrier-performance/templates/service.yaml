apiVersion: v1
kind: Service
metadata:
  name: {{ include "carrier-performance.fullname" . }}
  labels: {{- include "carrier-performance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "carrier-performance.selectorLabels" . | nindent 4 }}
