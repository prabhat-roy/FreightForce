apiVersion: v1
kind: Service
metadata:
  name: {{ include "temperature-monitoring.fullname" . }}
  labels: {{- include "temperature-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "temperature-monitoring.selectorLabels" . | nindent 4 }}
