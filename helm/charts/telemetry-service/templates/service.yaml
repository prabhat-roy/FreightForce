apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetry-service.fullname" . }}
  labels: {{- include "telemetry-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "telemetry-service.selectorLabels" . | nindent 4 }}
