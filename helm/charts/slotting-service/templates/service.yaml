apiVersion: v1
kind: Service
metadata:
  name: {{ include "slotting-service.fullname" . }}
  labels: {{- include "slotting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "slotting-service.selectorLabels" . | nindent 4 }}
