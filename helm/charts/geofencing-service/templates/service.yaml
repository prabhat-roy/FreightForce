apiVersion: v1
kind: Service
metadata:
  name: {{ include "geofencing-service.fullname" . }}
  labels: {{- include "geofencing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "geofencing-service.selectorLabels" . | nindent 4 }}
