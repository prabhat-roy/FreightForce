apiVersion: v1
kind: Service
metadata:
  name: {{ include "onboarding-service.fullname" . }}
  labels: {{- include "onboarding-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "onboarding-service.selectorLabels" . | nindent 4 }}
