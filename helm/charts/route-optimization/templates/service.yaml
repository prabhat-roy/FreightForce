apiVersion: v1
kind: Service
metadata:
  name: {{ include "route-optimization.fullname" . }}
  labels: {{- include "route-optimization.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "route-optimization.selectorLabels" . | nindent 4 }}
