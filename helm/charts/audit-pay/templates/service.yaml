apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-pay.fullname" . }}
  labels: {{- include "audit-pay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "audit-pay.selectorLabels" . | nindent 4 }}
