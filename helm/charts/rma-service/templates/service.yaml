apiVersion: v1
kind: Service
metadata:
  name: {{ include "rma-service.fullname" . }}
  labels: {{- include "rma-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rma-service.selectorLabels" . | nindent 4 }}
