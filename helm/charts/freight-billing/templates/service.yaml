apiVersion: v1
kind: Service
metadata:
  name: {{ include "freight-billing.fullname" . }}
  labels: {{- include "freight-billing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "freight-billing.selectorLabels" . | nindent 4 }}
