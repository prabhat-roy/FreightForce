apiVersion: v1
kind: Service
metadata:
  name: {{ include "serial-tracking.fullname" . }}
  labels: {{- include "serial-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "serial-tracking.selectorLabels" . | nindent 4 }}
