apiVersion: v1
kind: Service
metadata:
  name: {{ include "carbon-tracker.fullname" . }}
  labels: {{- include "carbon-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "carbon-tracker.selectorLabels" . | nindent 4 }}
