apiVersion: v1
kind: Service
metadata:
  name: {{ include "demand-forecasting.fullname" . }}
  labels: {{- include "demand-forecasting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "demand-forecasting.selectorLabels" . | nindent 4 }}
