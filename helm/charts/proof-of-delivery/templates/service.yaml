apiVersion: v1
kind: Service
metadata:
  name: {{ include "proof-of-delivery.fullname" . }}
  labels: {{- include "proof-of-delivery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "proof-of-delivery.selectorLabels" . | nindent 4 }}
