apiVersion: v1
kind: Service
metadata:
  name: {{ include "wms-service.fullname" . }}
  labels: {{- include "wms-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "wms-service.selectorLabels" . | nindent 4 }}
