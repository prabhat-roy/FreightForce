cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: freightforce-geo-address-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com
