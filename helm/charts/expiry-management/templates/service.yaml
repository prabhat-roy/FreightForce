apiVersion: v1
kind: Service
metadata:
  name: {{ include "expiry-management.fullname" . }}
  labels: {{- include "expiry-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "expiry-management.selectorLabels" . | nindent 4 }}
