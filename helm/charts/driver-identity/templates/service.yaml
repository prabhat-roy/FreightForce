apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-identity.fullname" . }}
  labels: {{- include "driver-identity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "driver-identity.selectorLabels" . | nindent 4 }}
