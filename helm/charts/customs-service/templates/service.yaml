apiVersion: v1
kind: Service
metadata:
  name: {{ include "customs-service.fullname" . }}
  labels: {{- include "customs-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "customs-service.selectorLabels" . | nindent 4 }}
