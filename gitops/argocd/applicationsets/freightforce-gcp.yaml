apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata: { name: freightforce-gcp, namespace: argocd }
spec:
  generators:
    - git:
        repoURL: https://github.com/prabhat-roy/FreightForce.git
        revision: HEAD
        directories: [{ path: helm/charts/* }]
  template:
    metadata:
      name: '{{path.basename}}-gcp'
      labels: { cloud: gcp, freightforce.io/managed-by: argocd }
    spec:
      project: freightforce
      source:
        repoURL: https://github.com/prabhat-roy/FreightForce.git
        targetRevision: HEAD
        path: '{{path}}'
        helm:
          valueFiles: [values.yaml, values-gcp.yaml]
      destination:
        server: https://kubernetes.default.svc
        namespace: freightforce
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
