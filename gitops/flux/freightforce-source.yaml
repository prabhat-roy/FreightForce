apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: freightforce, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/FreightForce.git
  ref: { branch: main }
