name: freightforce
description: FreightForce — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://freightforce-pulumi-state
config:
  freightforce:env: prod
  freightforce:cloud: aws
