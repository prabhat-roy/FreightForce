apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: freightforce-vault-issuer }
spec:
  vault:
    server: https://vault.freightforce.internal
    path: pki/sign/freightforce-mesh
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
        secretRef: { name: cert-manager-vault-token, key: token }
