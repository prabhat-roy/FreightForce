global:
  identity:
    auth: { issuerBackendUrl: https://keycloak.freightforce.internal/realms/camunda-platform }
zeebe:           { clusterSize: 3, partitionCount: 3 }
zeebe-gateway:   { replicas: 2 }
operate:         { replicas: 1 }
tasklist:        { replicas: 1 }
optimize:        { replicas: 1 }
identity:        { ingress: { enabled: true, host: camunda.freightforce.internal } }
elasticsearch:   { master: { replicaCount: 3 } }
