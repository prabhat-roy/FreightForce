global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.freightforce.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.freightforce.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
