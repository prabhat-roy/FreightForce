apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: freightforce-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [freightforce] }
  timeOffset: "+500ms"
  duration: 3m
