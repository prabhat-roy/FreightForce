opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: freightforce-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.freightforce.internal, paths: [{ path: /, pathType: Prefix }] }]
